import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EthicsGate | Modern IRB Review Platform",
  description: "Streamline your institution's ethics review process with inline annotations, real-time collaboration, and a beautiful modern interface.",
  keywords: ["IRB", "ethics review", "research", "institutional review board", "proposal management"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
